require 'spec_helper'

describe Conjur::Asset::Authn::Local do
  it 'has a version number' do
    expect(Conjur::Asset::Authn::Local::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
